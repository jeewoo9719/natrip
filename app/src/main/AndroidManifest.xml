<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chanyoung.nattrip">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SignIn" />
        <activity
            android:name=".ChatList"
            android:label="@string/title_activity_chat_list" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".RegisterTourActivity"
            android:label="RegisterTourActivity" />
        <activity
            android:name=".ModiInfoActivity"
            android:label="ModiInfoActivity" />
        <activity
            android:name=".InsertImage"
            android:label="@string/title_activity_insert_image" />
        <activity
            android:name=".SearchActivity"
            android:label="SearchActivity" />
        <activity
            android:name=".SettingActivity"
            android:label="SettingActivity" />
        <activity
            android:name=".TourActivity"
            android:label="@string/title_activity_tour" />
        <activity android:name=".regist_guide" />
        <activity android:name=".register_list"></activity>
    </application>

</manifest>